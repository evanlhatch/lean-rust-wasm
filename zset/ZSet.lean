/-
# ZSet — the delta substrate's umbrella (the Change root's additive foundation)

Owned by: the ZSet agent (the mandate tree, `zset/`).

The five questions (notes/v3/01-core.md):
- **Root**: Change (01 §2) — the delta ladder's top rung's canonical
  occupant: `ZSet` is a `Kit.Change.Additive` (delta-as-state, dbsp).
- **Carrier grade**: the canonical-rep structure over a sorted zero-free
  association list (06 §7c) — NOT a quotient; the wire/guest layers
  carry `toList` as THE representation.
- **Spine reading**: none — the substrate the spine's lanes ride.
- **Ladder rung**: 6 — `zsetAdditive` (every field discharged from the
  group laws; no parallel operations).
- **Gate rows**: the axiom report + ZSetTests (positive pins + the
  mandatory negative controls + the trichotomy's discipline pins).

Parts: `ZSet.Basic` (the ONE canonical-rep machinery — `CanonKey` +
`WKind` + `Weighted K Row` with canonicity + the operators + the ℤ
surface `ZSet α := Weighted Int α` with the group laws + the Additive
rung), `ZSet.Trichotomy` (command/delta/event as data — 03 §7's
trichotomy, never collapsed), `ZSet.Relation` (the query-fragment
layer: positivity, the non-linear distinct/threshold old-values rule,
THE ONE THEOREM `evaluate_wmap`, the ℤ→Bool bag-restricted collapse,
the identity `ofZSet` bridge), `ZSet.Graph` (the graph-READING layer:
a directed graph IS a weighted relation over `V × V` — the edge/
adjacency accessors, `Reach` + the bounded path-folding sweep,
`Acyclic` with the cycle refused as data, the strata `peel`).
-/

import ZSet.Basic
import ZSet.Trichotomy
import ZSet.Relation
import ZSet.Graph
