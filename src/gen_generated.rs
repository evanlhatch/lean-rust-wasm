// GENERATED by schema-lang (lean-4.33.0) at 2026-09-22T14:39Z — DO NOT EDIT
// spec 0f43de3+ | Demo.lean + FeatureFlags.lean (@[schema] records; cedar-study.md Generators) | 16 item(s) | content hash 13212283962536331687 | regen: just gen; drift fails CI
// Rust VALUE GENERATORS emitted from the schema registry (W6.4 phase 1).
// Shape: cedar-proven (cedar-study.md Generators) — explicit max_depth
// budget with forced leaf at 0, a collision pool on strings (90/10),
// no derive(Arbitrary) on recursive shapes. Consume with
// arbitrary::Unstructured (bolero's with_arbitrary works too).
use arbitrary::Unstructured;

// The generated domain types (the sibling artifact — schema_generated.rs).
use crate::schema_generated::*;

/// The collision pool: the registry's record names. 90% of generated
/// strings are pool members (shared referents — joins/refs can fire);
/// 10% are fresh. Derived from the spec, never hand-listed.
pub const STRING_POOL: &[&str] = &["User", "OrderItem", "Order", "Flag", "AuditEntry"];

/// A string: 90% a pool member, 10% fresh (<= 8 lowercase chars —
/// small supplies shrink well, the Gen.lean `genChar` discipline).
pub fn gen_string(u: &mut Unstructured<'_>) -> arbitrary::Result<String> {
    if u.ratio(9, 10)? {
        return Ok(u.choose(STRING_POOL)?.to_string());
    }
    let len = u.int_in_range(0..=8usize)?;
    let mut s = String::with_capacity(len);
    for _ in 0..len {
        s.push(char::from(b'a' + u.int_in_range(0..=25u8)?));
    }
    Ok(s)
}

/// Bytes: a short bounded vec (the byte guard is Unstructured's own `len`).
pub fn gen_bytes(u: &mut Unstructured<'_>) -> arbitrary::Result<Vec<u8>> {
    let len = u.int_in_range(0..=8usize)?;
    let mut v = Vec::with_capacity(len);
    for _ in 0..len {
        v.push(u.arbitrary()?);
    }
    Ok(v)
}

/// A list: forced EMPTY at depth 0, else <= 3 elements at depth - 1.
pub fn gen_list<T>(
    u: &mut Unstructured<'_>,
    max_depth: usize,
    elem: impl Fn(&mut Unstructured<'_>, usize) -> arbitrary::Result<T>,
) -> arbitrary::Result<Vec<T>> {
    let len = if max_depth == 0 { 0 } else { u.int_in_range(0..=3usize)? };
    let mut v = Vec::with_capacity(len);
    for _ in 0..len {
        v.push(elem(u, max_depth - 1)?);
    }
    Ok(v)
}

/// An option: forced `None` at depth 0, else a coin flip (the Some arm
/// spends the depth).
pub fn gen_option<T>(
    u: &mut Unstructured<'_>,
    max_depth: usize,
    inner: impl Fn(&mut Unstructured<'_>, usize) -> arbitrary::Result<T>,
) -> arbitrary::Result<Option<T>> {
    if max_depth == 0 || u.ratio(1, 2)? {
        return Ok(None);
    }
    Ok(Some(inner(u, max_depth - 1)?))
}

pub fn gen_user(u: &mut Unstructured<'_>, max_depth: usize) -> arbitrary::Result<User> {
  Ok(User {
      id: u.arbitrary()?,
      name: gen_string(u)?,
      email: gen_string(u)?,
      tags: gen_list(u, max_depth, |u, d| Ok(gen_string(u)?))?,
    })
}
pub fn gen_order_item(u: &mut Unstructured<'_>, max_depth: usize) -> arbitrary::Result<OrderItem> {
  Ok(OrderItem {
      id: u.arbitrary()?,
      qty: u.arbitrary()?,
      price: u.arbitrary()?,
    })
}
pub fn gen_order(u: &mut Unstructured<'_>, max_depth: usize) -> arbitrary::Result<Order> {
  Ok(Order {
      id: u.arbitrary()?,
      items: gen_list(u, max_depth, |u, d| Ok(gen_order_item(u, d.saturating_sub(1))?))?,
      total: u.arbitrary()?,
    })
}
pub fn gen_flag(u: &mut Unstructured<'_>, max_depth: usize) -> arbitrary::Result<Flag> {
  Ok(Flag {
      id: u.arbitrary()?,
      key: gen_string(u)?,
      enabled: u.arbitrary()?,
      rollout: u.arbitrary()?,
      tags: gen_list(u, max_depth, |u, d| Ok(gen_string(u)?))?,
    })
}
pub fn gen_audit_entry(u: &mut Unstructured<'_>, max_depth: usize) -> arbitrary::Result<AuditEntry> {
  Ok(AuditEntry {
      id: u.arbitrary()?,
      flagKey: gen_string(u)?,
      action: gen_string(u)?,
      ts: u.arbitrary()?,
    })
}
