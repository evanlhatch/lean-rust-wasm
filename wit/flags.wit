// GENERATED by schema-lang (lean-4.33.0) at 2026-09-15T22:54Z — DO NOT EDIT
// spec f04ad66+ | FeatureFlags.lean (via GenCtx.rootPartitionOf) | 16 item(s) | content hash 11837098649252792748 | regen: just gen; drift fails CI
package guestlang:flags;

interface flags-types {
record flag {
  id: u64,
  key: string,
  enabled: bool,
  rollout: u64,
  tags: list<string>,
}
record audit-entry {
  id: u64,
  flag-key: string,
  action: string,
  ts: u64,
}
variant flag-error {
  not-found,
  invalid-rollout(u64),
  locked(string),
}
resource flag-store;
}

interface flags-exports {
  use flags-types.{flag, flag-error, audit-entry};
    flag-get: func(id: u64) -> option<flag>;
    flag-set-rollout: func(id: u64, pct: u64) -> result<flag, flag-error>;
    flag-delete: func(id: u64) -> result<audit-entry, flag-error>;
    flag-watch: async func(key: string) -> list<flag>;
}

world flags {
  export flags-exports;
}
