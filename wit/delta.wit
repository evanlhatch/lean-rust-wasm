// GENERATED by schema-lang (lean-4.33.0) at 2026-09-16T12:04Z — DO NOT EDIT
// spec 7bd8583+ | Demo.lean | 16 item(s) | content hash 2522213127418637669 | regen: just gen; drift fails CI
variant user-change {
    insert(user),
    update(user),
    remove(u64),
}
variant order-item-change {
    insert(order-item),
    update(order-item),
    remove(u64),
}
variant order-change {
    insert(order),
    update(order),
    remove(u64),
}
variant flag-change {
    insert(flag),
    update(flag),
    remove(u64),
}
variant audit-entry-change {
    insert(audit-entry),
    update(audit-entry),
    remove(u64),
}
