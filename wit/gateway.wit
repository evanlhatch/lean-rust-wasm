// GENERATED by schema-lang (lean-4.33.0) at 2026-09-16T10:36Z — DO NOT EDIT
// spec 0793881+ | Demo.lean | 16 item(s) | content hash 15753156408862744284 | regen: just gen; drift fails CI
package demo:gateway;

interface gateway-types {
record user {
  id: u64,
  name: string,
  email: string,
  tags: list<string>,
}
record order-item {
  id: u64,
  qty: u32,
  price: f64,
}
record order {
  id: u64,
  items: list<order-item>,
  total: f64,
}
variant role {
  admin,
  editor,
  viewer,
}
variant order-error {
  empty-cart,
  invalid-item(u64),
  insufficient-funds(f64),
}
resource db;
}

interface gateway-exports {
  use gateway-types.{user, order-error};
    get-user: func(id: u64) -> option<user>;
    watch-orders: async func(into: order-error) -> list<user>;
}

world gateway {
  export gateway-exports;
}
