// GENERATED by schema-lang — DO NOT EDIT
// spec source: SchemaLang/Spec/Demo.lean
// regenerate via `just gen`; drift fails CI (byte-tie)
package demo:gateway;

world gateway {
record user {
  id: u64,
  name: string,
  email: string,
  tags: list<string>,
}
variant role {
  admin,
  editor,
  viewer,
}
variant order-error {
  empty-cart,
  invalid-item(u64),
  insufficient-funds(f64),
}
resource db;
  export interface gateway-exports {
  get-user: func(id: u64) -> option<user>;
  watch-orders: func(into: order-error) -> future<list<user>>;
  }
}
