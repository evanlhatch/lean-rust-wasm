/-
# Circuit — the incremental circuit lane's umbrella (03-bidirectional §8-9)

Owned by: the Circuit agent (the mandate tree, `circuit/`).

The five questions (notes/v3/01-core.md):
- **Root**: Change (01 §2) — the derivative discipline's circuit face:
  the constraint/violation relations' INCREMENTAL maintainer's
  foundation (03 §8: the state invariant's violation query, maintained
  incrementally, with `incrementalize_ok` proving the maintained result
  equals the full recomputation ONCE, at the circuit theory).
- **Carrier grade**: the Z-set canonical-rep relation consumed, never
  re-carried; the circuit is pure structure with two readings
  (whole-input denotation / stepped delta).
- **Spine reading**: none — the Ckt lane's seed substrate.
- **Ladder rung**: hand theorems of the structural-induction kind.
- **Gate rows**: the axiom report + CircuitTests (the worked agreement,
  the cross-term teeth, the old-values teeth, the mandatory negative
  controls).

Parts: `Circuit.Basic` (the operator templates + the circuit + THE
agreement theorem `Ckt.incrementalize_ok` + the rewrite pass
`recursive_opt_ok` — the mined dbsp certification template, ported
fresh over the Z-set substrate), `Circuit.Compile` (the query
fragment's compilation + the agreement — the substrate's `ZSet.Query`
grammar; the query LANE's typed fragment is the type-indexed circuit's
named follow-up, the boundary reported in Compile's header).

Core-only (imports ZSet — the cone rule).
-/

import Circuit.Basic
import Circuit.Compile
