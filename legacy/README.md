The pre-v3 tree — the mining source for the macht build. Read-only:
nothing here builds in this home (the move broke its paths by design);
its gates and builds run only from its own history (`jj`). Content is
mined out of it port-by-port per `notes/v3/14-build-map.md`; it is
never edited, never migrated in place, and never deleted — `jj` keeps
the full history.
