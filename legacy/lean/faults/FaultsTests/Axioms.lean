/- Axiom gate: headline theorems must depend only on the core triple. -/
import Faults
#print axioms Faults.allocate_length
-- The ladder-audit bridge: the faults-local iff, composed pointwise
-- from the Wf lane's `tyCheck_eq_nil_iff` (the universe bridge's
-- transport). Bar: the core triple.
#print axioms Faults.FailureModeItem.wellFormed_iff
