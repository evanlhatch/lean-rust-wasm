variant user-change {
    insert(user),
    update(user),
    remove(u64),
}
variant order-item-change {
    insert(order-item),
    update(order-item),
    remove(u64),
}
variant order-change {
    insert(order),
    update(order),
    remove(u64),
}
