[package]
name = "forge"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
# Pipeline orchestrator: lake emitters -> byte-tie -> component link -> OCI.
# Deps land with each phase (serde, sha2, oci-wasm, wasm-encoder...).
serde_json = "1"
xxhash-rust = { version = "0.8", features = ["xxh3"] }
