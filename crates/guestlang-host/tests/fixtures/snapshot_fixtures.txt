# GENERATED by schema-lang's snapshot-fixtures exe — DO NOT EDIT
# seed 20261119 | count 32 | regen: just snapshot-fixtures (byte-identical)
# pair: `== u<i>` header, snapshot text after `snap:`, the dump of
# Lean's parse output after `expect:` (see Tests/SnapshotRT.lean).
== u0
snap:
variant gamma
case id
case name
func OrderItem
param unitPrice i64
ret tensor(3;5;u64)
sem custom volatile stream
resource OrderItem
variant getUser
case unitPrice
expect:
item variant gamma
cs id
cs name
item func OrderItem
p unitPrice i64
ret ( tensor 3 5 u64 )
sem custom volatile stream
item resource OrderItem
item variant getUser
cs unitPrice

== u1
snap:
func OrderItem
param qty u64
param unitPrice set(i64)
ret result(option(map(u32,list(option(set(string))))),result(i8,u8))
sem strict volatile
variant alpha
case qty
case id list(result(tensor(5;3;0;i64),tensor(0;list(stream(set(i32))))))
case id
variant alpha
case id i8
case unitPrice
resource getUser
expect:
item func OrderItem
p qty u64
p unitPrice ( set i64 )
ret ( result ( option ( map u32 ( list ( option ( set string ) ) ) ) ) ( result i8 u8 ) )
sem strict volatile
item variant alpha
cs qty
cs id ( list ( result ( tensor 5 3 0 i64 ) ( tensor 0 ( list ( stream ( set i32 ) ) ) ) ) )
cs id
item variant alpha
cs id i8
cs unitPrice
item resource getUser

== u2
snap:
variant beta
record OrderItem
field qty map(i16,u8)
record gamma
field name set(i8)
field unitPrice set(i8)
func OrderItem
param name set(string)
ret bytes
sem strict volatile stream
expect:
item variant beta
item record OrderItem
fld qty ( map i16 u8 )
item record gamma
fld name ( set i8 )
fld unitPrice ( set i8 )
item func OrderItem
p name ( set string )
ret bytes
sem strict volatile stream

== u3
snap:
variant gamma
func beta
param id tensor(2;1;1;tensor(3;i16))
ret future(result(option(result(future(option(future(option(option(map(i64,i64)))))),result(f32,u16))),tensor(2;0;list(list(i32)))))
sem custom pure
variant beta
record getUser
expect:
item variant gamma
item func beta
p id ( tensor 2 1 1 ( tensor 3 i16 ) )
ret ( future ( result ( option ( result ( future ( option ( future ( option ( option ( map i64 i64 ) ) ) ) ) ) ( result f32 u16 ) ) ) ( tensor 2 0 ( list ( list i32 ) ) ) ) )
sem custom pure
item variant beta
item record getUser

== u4
snap:
func beta
param id stream(f32)
param qty f64
ret result(stream(tensor(5;u8)),set(u16))
sem custom pure stream
record getUser
field unitPrice u16
field name future(set(string))
field unitPrice bytes
variant beta
case id option(option(stream(set(i8))))
case qty
case id u16
resource gamma
expect:
item func beta
p id ( stream f32 )
p qty f64
ret ( result ( stream ( tensor 5 u8 ) ) ( set u16 ) )
sem custom pure stream
item record getUser
fld unitPrice u16
fld name ( future ( set string ) )
fld unitPrice bytes
item variant beta
cs id ( option ( option ( stream ( set i8 ) ) ) )
cs qty
cs id u16
item resource gamma

== u5
snap:
resource beta
func alpha
param unitPrice stream(map(i32,set(i32)))
param unitPrice tensor(1;result(f32,stream(string)))
ret set(u8)
sem strict stable
resource getUser
variant gamma
expect:
item resource beta
item func alpha
p unitPrice ( stream ( map i32 ( set i32 ) ) )
p unitPrice ( tensor 1 ( result f32 ( stream string ) ) )
ret ( set u8 )
sem strict stable
item resource getUser
item variant gamma

== u6
snap:
func alpha
ret stream(option(future(i8)))
sem strict pure stream
variant gamma
variant beta
case id
case unitPrice
variant beta
case unitPrice
case id
case name
expect:
item func alpha
ret ( stream ( option ( future i8 ) ) )
sem strict pure stream
item variant gamma
item variant beta
cs id
cs unitPrice
item variant beta
cs unitPrice
cs id
cs name

== u7
snap:
func getUser
param qty i16
ret map(string,map(u8,stream(option(tensor(5;4;4;i64)))))
sem strict pure stream
variant beta
case id
case id
case id
resource alpha
record beta
expect:
item func getUser
p qty i16
ret ( map string ( map u8 ( stream ( option ( tensor 5 4 4 i64 ) ) ) ) )
sem strict pure stream
item variant beta
cs id
cs id
cs id
item resource alpha
item record beta

== u8
snap:
resource getUser
resource gamma
variant gamma
case unitPrice
func alpha
ret stream(u8)
sem strict volatile stream
expect:
item resource getUser
item resource gamma
item variant gamma
cs unitPrice
item func alpha
ret ( stream u8 )
sem strict volatile stream

== u9
snap:
record OrderItem
field qty tensor(0;2;map(i16,set(i64)))
field unitPrice list(list(string))
record alpha
field name option(future(list(option(tensor(1;stream(u16))))))
resource gamma
record OrderItem
expect:
item record OrderItem
fld qty ( tensor 0 2 ( map i16 ( set i64 ) ) )
fld unitPrice ( list ( list string ) )
item record alpha
fld name ( option ( future ( list ( option ( tensor 1 ( stream u16 ) ) ) ) ) )
item resource gamma
item record OrderItem

== u10
snap:
resource OrderItem
variant OrderItem
case id
variant beta
case unitPrice future(i8)
case id future(map(i8,i64))
case unitPrice set(string)
func getUser
param name set(i32)
param unitPrice i16
ret list(map(u8,i64))
sem strict stable stream
expect:
item resource OrderItem
item variant OrderItem
cs id
item variant beta
cs unitPrice ( future i8 )
cs id ( future ( map i8 i64 ) )
cs unitPrice ( set string )
item func getUser
p name ( set i32 )
p unitPrice i16
ret ( list ( map u8 i64 ) )
sem strict stable stream

== u11
snap:
resource gamma
func getUser
ret result(u64,tensor(2;5;future(map(u32,option(option(set(string)))))))
sem strict stable
func gamma
ret i32
sem propagate volatile stream
resource gamma
expect:
item resource gamma
item func getUser
ret ( result u64 ( tensor 2 5 ( future ( map u32 ( option ( option ( set string ) ) ) ) ) ) )
sem strict stable
item func gamma
ret i32
sem propagate volatile stream
item resource gamma

== u12
snap:
record beta
record beta
field unitPrice list(option(stream(ty(OrderItem))))
field qty u32
field unitPrice f32
func alpha
param qty set(string)
ret option(ty(gamma))
variant gamma
expect:
item record beta
item record beta
fld unitPrice ( list ( option ( stream ( ref OrderItem ) ) ) )
fld qty u32
fld unitPrice f32
item func alpha
p qty ( set string )
ret ( option ( ref gamma ) )
item variant gamma

== u13
snap:
variant beta
case qty
case name
case name string
func OrderItem
param qty stream(option(u64))
ret option(future(map(u32,option(i32))))
sem strict stable stream
func OrderItem
param name option(f64)
param name u32
ret set(u8)
sem custom volatile stream
func gamma
param name option(list(tensor(4;u8)))
ret future(u32)
sem custom pure stream
expect:
item variant beta
cs qty
cs name
cs name string
item func OrderItem
p qty ( stream ( option u64 ) )
ret ( option ( future ( map u32 ( option i32 ) ) ) )
sem strict stable stream
item func OrderItem
p name ( option f64 )
p name u32
ret ( set u8 )
sem custom volatile stream
item func gamma
p name ( option ( list ( tensor 4 u8 ) ) )
ret ( future u32 )
sem custom pure stream

== u14
snap:
record alpha
field unitPrice i64
field id option(option(bytes))
record OrderItem
record gamma
field unitPrice i16
field name option(i64)
field id ty(gamma)
variant gamma
expect:
item record alpha
fld unitPrice i64
fld id ( option ( option bytes ) )
item record OrderItem
item record gamma
fld unitPrice i16
fld name ( option i64 )
fld id ( ref gamma )
item variant gamma

== u15
snap:
record beta
field qty f64
record gamma
field qty option(tensor(1;1;1;list(option(set(i8)))))
resource OrderItem
resource beta
expect:
item record beta
fld qty f64
item record gamma
fld qty ( option ( tensor 1 1 1 ( list ( option ( set i8 ) ) ) ) )
item resource OrderItem
item resource beta

== u16
snap:
record getUser
field id set(i16)
variant gamma
case qty
case qty u64
func alpha
ret result(future(set(u16)),option(i16))
sem strict pure stream
record OrderItem
field qty i8
field id future(stream(map(u64,map(u32,u16))))
expect:
item record getUser
fld id ( set i16 )
item variant gamma
cs qty
cs qty u64
item func alpha
ret ( result ( future ( set u16 ) ) ( option i16 ) )
sem strict pure stream
item record OrderItem
fld qty i8
fld id ( future ( stream ( map u64 ( map u32 u16 ) ) ) )

== u17
snap:
func alpha
ret option(stream(future(map(i8,i8))))
sem propagate pure stream
func alpha
ret stream(future(i32))
sem propagate volatile
func OrderItem
param name option(ty(gamma))
param id set(i32)
ret bytes
func beta
param unitPrice result(option(option(map(string,option(future(option(f32)))))),list(stream(set(u32))))
ret set(i16)
sem strict volatile stream
expect:
item func alpha
ret ( option ( stream ( future ( map i8 i8 ) ) ) )
sem propagate pure stream
item func alpha
ret ( stream ( future i32 ) )
sem propagate volatile
item func OrderItem
p name ( option ( ref gamma ) )
p id ( set i32 )
ret bytes
item func beta
p unitPrice ( result ( option ( option ( map string ( option ( future ( option f32 ) ) ) ) ) ) ( list ( stream ( set u32 ) ) ) )
ret ( set i16 )
sem strict volatile stream

== u18
snap:
variant OrderItem
case id
case name
case id
resource gamma
func OrderItem
param unitPrice list(i32)
ret future(option(list(set(i32))))
sem custom volatile stream
record OrderItem
expect:
item variant OrderItem
cs id
cs name
cs id
item resource gamma
item func OrderItem
p unitPrice ( list i32 )
ret ( future ( option ( list ( set i32 ) ) ) )
sem custom volatile stream
item record OrderItem

== u19
snap:
variant gamma
case name
case unitPrice
record OrderItem
field unitPrice option(future(set(i8)))
variant getUser
case unitPrice option(result(option(option(f64)),set(u8)))
resource getUser
expect:
item variant gamma
cs name
cs unitPrice
item record OrderItem
fld unitPrice ( option ( future ( set i8 ) ) )
item variant getUser
cs unitPrice ( option ( result ( option ( option f64 ) ) ( set u8 ) ) )
item resource getUser

== u20
snap:
variant OrderItem
case qty u32
resource getUser
record getUser
field unitPrice option(i16)
field id i32
field unitPrice f64
variant beta
case unitPrice bytes
case id option(f32)
case name option(stream(option(result(i8,i8))))
expect:
item variant OrderItem
cs qty u32
item resource getUser
item record getUser
fld unitPrice ( option i16 )
fld id i32
fld unitPrice f64
item variant beta
cs unitPrice bytes
cs id ( option f32 )
cs name ( option ( stream ( option ( result i8 i8 ) ) ) )

== u21
snap:
resource beta
variant beta
case id string
func alpha
param unitPrice u8
ret i64
sem custom stable stream
variant getUser
case id
case name set(i8)
case name option(future(map(i32,u32)))
expect:
item resource beta
item variant beta
cs id string
item func alpha
p unitPrice u8
ret i64
sem custom stable stream
item variant getUser
cs id
cs name ( set i8 )
cs name ( option ( future ( map i32 u32 ) ) )

== u22
snap:
func gamma
ret future(tensor(3;u64))
sem strict pure
resource OrderItem
func alpha
param id i32
ret set(i8)
sem custom stable stream
func gamma
param qty map(string,ty(OrderItem))
ret i8
sem strict pure
expect:
item func gamma
ret ( future ( tensor 3 u64 ) )
sem strict pure
item resource OrderItem
item func alpha
p id i32
ret ( set i8 )
sem custom stable stream
item func gamma
p qty ( map string ( ref OrderItem ) )
ret i8
sem strict pure

== u23
snap:
func beta
param id map(u32,option(string))
param qty tensor(3;1;stream(option(set(u64))))
ret stream(option(result(tensor(1;1;f32),u16)))
sem propagate volatile stream
func OrderItem
param id tensor(5;0;2;i16)
param name u64
ret map(u8,option(option(f32)))
sem strict stable
variant OrderItem
case id
resource alpha
expect:
item func beta
p id ( map u32 ( option string ) )
p qty ( tensor 3 1 ( stream ( option ( set u64 ) ) ) )
ret ( stream ( option ( result ( tensor 1 1 f32 ) u16 ) ) )
sem propagate volatile stream
item func OrderItem
p id ( tensor 5 0 2 i16 )
p name u64
ret ( map u8 ( option ( option f32 ) ) )
sem strict stable
item variant OrderItem
cs id
item resource alpha

== u24
snap:
func beta
param unitPrice future(map(i8,i32))
ret stream(u8)
sem strict volatile
record beta
field id map(string,list(set(i8)))
field name u8
field id set(u32)
resource gamma
variant alpha
case id option(i64)
case name
expect:
item func beta
p unitPrice ( future ( map i8 i32 ) )
ret ( stream u8 )
sem strict volatile
item record beta
fld id ( map string ( list ( set i8 ) ) )
fld name u8
fld id ( set u32 )
item resource gamma
item variant alpha
cs id ( option i64 )
cs name

== u25
snap:
record alpha
variant beta
case id option(option(i64))
case unitPrice
case id i16
resource beta
func alpha
param name ty(OrderItem)
ret option(option(set(u32)))
sem custom pure
expect:
item record alpha
item variant beta
cs id ( option ( option i64 ) )
cs unitPrice
cs id i16
item resource beta
item func alpha
p name ( ref OrderItem )
ret ( option ( option ( set u32 ) ) )
sem custom pure

== u26
snap:
resource alpha
variant beta
variant getUser
case name
func beta
ret map(string,map(i32,map(i64,future(i16))))
sem strict volatile
expect:
item resource alpha
item variant beta
item variant getUser
cs name
item func beta
ret ( map string ( map i32 ( map i64 ( future i16 ) ) ) )
sem strict volatile

== u27
snap:
resource getUser
resource alpha
func beta
param id option(option(f32))
ret option(ty(OrderItem))
sem custom volatile stream
func beta
ret ty(getUser)
expect:
item resource getUser
item resource alpha
item func beta
p id ( option ( option f32 ) )
ret ( option ( ref OrderItem ) )
sem custom volatile stream
item func beta
ret ( ref getUser )

== u28
snap:
resource OrderItem
func gamma
param id i64
param name tensor(list(option(stream(result(i32,u32)))))
ret option(future(u64))
sem strict pure stream
func alpha
ret tensor(3;list(list(option(map(u8,u8)))))
sem propagate volatile stream
resource gamma
expect:
item resource OrderItem
item func gamma
p id i64
p name ( tensor ( list ( option ( stream ( result i32 u32 ) ) ) ) )
ret ( option ( future u64 ) )
sem strict pure stream
item func alpha
ret ( tensor 3 ( list ( list ( option ( map u8 u8 ) ) ) ) )
sem propagate volatile stream
item resource gamma

== u29
snap:
record OrderItem
field id set(u32)
func beta
param name option(ty(gamma))
ret future(bytes)
sem propagate stable stream
variant OrderItem
case id
resource gamma
expect:
item record OrderItem
fld id ( set u32 )
item func beta
p name ( option ( ref gamma ) )
ret ( future bytes )
sem propagate stable stream
item variant OrderItem
cs id
item resource gamma

== u30
snap:
record gamma
resource getUser
resource beta
variant gamma
expect:
item record gamma
item resource getUser
item resource beta
item variant gamma

== u31
snap:
record beta
variant OrderItem
case unitPrice option(list(list(result(option(i64),future(result(future(option(future(tensor(5;1;tensor(2;2;set(string)))))),future(i8)))))))
variant OrderItem
func getUser
ret option(ty(beta))
sem custom stable stream
expect:
item record beta
item variant OrderItem
cs unitPrice ( option ( list ( list ( result ( option i64 ) ( future ( result ( future ( option ( future ( tensor 5 1 ( tensor 2 2 ( set string ) ) ) ) ) ) ( future i8 ) ) ) ) ) ) )
item variant OrderItem
item func getUser
ret ( option ( ref beta ) )
sem custom stable stream
