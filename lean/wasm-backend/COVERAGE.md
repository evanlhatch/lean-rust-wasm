# COVERAGE — the oracle's feature → test map

GENERATED by `lake exe oracle coverage` (lean/wasm-backend) — do not hand-edit.

Schema surface (the canonical string the verdict schema hash is sha256 of):

`double/1,is-big/1,adder/2,double-area/1,run-paps/1,total/3,pick/3,str-len-demo/1,greet/1,get-user/1,watch-counts/1,watch-users/1,user-valid/4,order-error-valid/2,user-complete/4`

## Per-export coverage

| export | arity | manifest rows | schema features exercised |
| --- | --- | --- | --- |
| `double` | 1 | 69 | `u64-scalar`, `wrapping-arith` |
| `is-big` | 1 | 73 | `u64-scalar`, `bool-result`, `threshold-branch` |
| `adder` | 2 | 72 | `u64-scalar`, `multi-arg`, `closure-pap`, `wrapping-arith` |
| `double-area` | 1 | 70 | `u64-scalar`, `ctor-dispatch`, `object-lifecycle` |
| `run-paps` | 1 | 79 | `closure-pap`, `closures-in-list`, `multi-apply` |
| `total` | 3 | 82 | `u64-scalar`, `multi-arg`, `curried-closure`, `tail-recursion` |
| `pick` | 3 | 93 | `bool-arg`, `escaping-closure`, `branchy-closure` |
| `str-len-demo` | 1 | 42 | `string-intrinsic`, `runtime-dependent-branch` |
| `greet` | 1 | 26 | `string-result`, `canonical-abi-return-area` |
| `get-user` | 1 | 25 | `option-result`, `record-result`, `list-of-string`, `sentinel-none` |
| `watch-counts` | 1 | 32 | `stream-u64`, `async-lift` |
| `watch-users` | 1 | 20 | `stream-record`, `async-lift` |
| `user-valid` | 4 | 40 | `record-arg`, `validator`, `negative-gate:id-zero` |
| `order-error-valid` | 2 | 13 | `variant-arg`, `validator`, `negative-empty-cart`, `f64-payload-arm` |
| `user-complete` | 4 | 38 | `record-arg`, `validator`, `strlen-gate`, `tags-count-gate` |

## Batch coverage (one shared context, five probe batches)

| batch | probes | adds |
| --- | --- | --- |
| `grid` | 287 | the fixed u64s grid × all 15 exports + the pinned validator negatives |
| `fuzz` | 200 | LCG-driven off-grid scalars — the engines must agree on inputs the grid never visits (wrap boundary) |
| `boundary` | 72 | u64 limits 0/1/2/2^31/2^32±1/2^63±1/max, adder/pick complement pairs |
| `sweep` | 120 | second LCG over the fns the first fuzz skips (str-len-demo, watch-counts) |
| `gen` | 95 | Plausible edge rows: nested-id sentinels, the "a,,b" splitOn edge, length boundaries, off-grid u64 leaves |

## Negative controls

- `user-valid` id=0 — the validator MUST refuse (Lean says false, the wasm must agree)
- `order-error-valid` empty-cart / invalid-item(0) — the documented variant negatives
- `user-complete` strlen(3/4) + tags-count gates — the boundary flips
- the DiffSpec corruption rows (unknown fn, arity drift) — Lean-side rejection is observed
- steel-host's flipped-instruction sabotage — the gate demonstrably catches a wrong engine

Discipline: every export above has ≥1 feature and ≥1 manifest row; every feature
names its export. Pinned Lean-side (Tests/Main.lean guards) — a vacuous row or an
unfeatureable export fails the build.
