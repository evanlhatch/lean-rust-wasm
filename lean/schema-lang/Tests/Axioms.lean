/- Axiom gate: headline theorems must depend only on the core triple.
   (schema-lang currently has no theorems of its own — EqAns evidence is
   computational; this file gates the imports so drift fails loudly.) -/
import SchemaLang
