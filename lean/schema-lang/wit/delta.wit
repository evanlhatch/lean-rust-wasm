// GENERATED by schema-lang — DO NOT EDIT
// spec source: Demo.lean
// regenerate via `just gen`; drift fails CI (byte-tie)
variant user-change {
    insert(user),
    update(user),
    remove(u64),
}
variant order-item-change {
    insert(order-item),
    update(order-item),
    remove(u64),
}
variant order-change {
    insert(order),
    update(order),
    remove(u64),
}
